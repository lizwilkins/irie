<% provide :title, "Drivers" %>

<table class="table">
  <tr>
    <td><b><%= link_to "Trip Assignment" %></b></td>
    <td><b>First Name</b></td>
    <td><b>Last Name</b></td>
    <td><b>Phone</b></td>
    <td><b>Supervisor</b></td>
  </tr>
  <% @drivers.each do |driver| %>
    <tr>
      <td><%=  %></td>
      <td><%=  %></td>
      <td><%=  %></td>
      <td><%=  %></td>
      <td><%= driver.supervisor %></td>
      <td><%= link_to 'Edit', edit_driver_path(driver) %></td>
      <td><%= link_to 'Delete', driver, :method => :delete, :confirm => "Are you sure you want to delete this driver?" %></td>
    </tr>
  <% end %>

</table>

<%= link_to 'Create driver', new_driver_path, :class => 'btn btn-primary' %>

